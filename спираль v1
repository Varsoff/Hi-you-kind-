import pygame
class circle:
    def __init__(self,x,y,rad,col):
        self.x = x
        self.y = y
        self.rad = rad
        self.col = col
        self.dir = 'right'
    def h(self):
        if self.dir == 'right':
            self.x += 1
            if self.x > W:
                self.dir = 'left'
        else:
            self.x -= 1
            if self.x < 0:
                self.dir = 'right'
    def d(self):
        pygame.draw.circle(win, self.col, (self.x, self.y), 50)
