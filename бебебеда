import tkinter as tk
import random
from tkinter import photoimage


def p():
    global pl

    c.delete('all')
    pla = (random.randint(1, N_X - 1) * s, random.randint(1, N_Y -1) * s)
    pl= c.create_image((pla[0], pla[1]), image=p2,anchor='nw')
    l.config(text='Найди выход')
    m.bind('<KeyPress>',k)




def m(obj,mx,my):
    xy = c.move(obj,mx,my)
    print(xy)
    if xy[0] <= 0:
        c.m(obj,w,0)
    if xy[0] >= w:
        c.m(obj, -w, 0)
    if xy[1] <= 0:
    if xy[1] <= 0:
